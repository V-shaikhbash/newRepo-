trigger:
- main

pool:
  vmImage: 'windows-latest'

jobs:
- job: RunPowerShell
  steps:
  - task: PowerShell@2
    inputs:
      targetType: 'inline'
      script: |
        Write-Host "Hello, World!"
